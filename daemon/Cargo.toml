[package]
name = "asnux-daemon"
version = "1.0.1"
edition = "2021"
description = "ASNUX Audio Daemon - Kernel module manager and audio routing"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
log = "0.4"
env_logger = "0.11"
ctrlc = "3.4"
